package tree;

import java.util.*;

public class treeDuplicate {

	public node root;
	public treeDuplicate(int data){
		root = new node(data);
	}
	
	public node duplicate(node root){
		node root2 = null;
		if(root != null)
		{
			LinkedList<node> queue = new LinkedList<node>();
			queue.add(root);
		
		}
		return root2;
	}
	
}
