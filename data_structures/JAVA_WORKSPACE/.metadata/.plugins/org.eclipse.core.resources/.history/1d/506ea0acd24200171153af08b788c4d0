package DP;

public class coinChange {
public int findCoinChange(int coinValue[], int n){
	int result = 0;
	if(n < 0)
		return 0;
	else if(n == 0)
		return 1;
	else
	{
		for(int i=0; i < coinValue.length; i++)
		{
			result+=findCoinChange(coinValue, (n-coinValue[i]));
		}
	}
}
}
